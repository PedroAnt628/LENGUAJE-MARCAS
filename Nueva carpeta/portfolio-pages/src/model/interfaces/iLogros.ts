export interface ILogro {
    id: number;
    year: string;
    title: string;
    description: string;
    category: string; // Ej: "Major", "Regional", "World"
    icon: string;     // Emoji o URL de imagen
}